5.7.3 - Вызов функции из f-строки.

Вызво функции можно использовать в качестве местозаполнителя в f-строке.
Пример:

print(f'Число равняется {random.randit(1, 100)}.')

f-строки полезны когда нужно отформатировать результат вызова.
Пример:

print(F'{random.randit(1, 1000):^10d}')